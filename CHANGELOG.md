# Changelog

## Unreleased
- add meal window guardrails, school-day constraints, and expanded feature vector for the scheduling engine
- introduce child profile defaults, weight migration to model version 2, and configurable meal offsets
- refine AI task categorization heuristics for relaxing and outdoor activities
- expose new profile settings in the web client and add regression tests for scheduling constraints
- sync shared type definitions and server schemas with meal windows, activity targets, and meal-tagged tasks, plus strengthen scheduler and AI unit coverage

